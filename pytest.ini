[pytest]
# Only collect tests under tests/ and named test_*.py
testpaths = tests
python_files = test_*.py

# Don't recurse into folders with one-off scripts
norecursedirs = experiments DEYOLO runs* .venv build dist
addopts = -ra
